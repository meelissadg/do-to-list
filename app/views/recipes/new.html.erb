<%= form_for(@recipe) do |f| %>

	<p>
		<%= f.label :Nome %></br>
		<%= f.text_field :name %>
	</p>

	<p>
		<%= f.label :Info %></br>
		<%= f.text_area :info %>

	</p>
<%= f.submit %>
<%end%>